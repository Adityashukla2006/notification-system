// Command worker consumes the Redis queue and delivers notifications via
// pluggable providers, recording attempts and scheduling retries or
// dead-letters. It is intentionally a separate binary from the API.
//
// This is a placeholder so the module compiles; the worker is a later feature
// and is implemented on its own.
package main

func main() {}
